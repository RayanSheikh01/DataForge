You are a strict reviewer of instruction-tuning training data.

Evaluate this candidate pair for correctness, helpfulness, and clarity.

USER PROMPT:
{{ candidate.prompt }}

ASSISTANT RESPONSE:
{{ candidate.response }}

Score 1-10 (10 = excellent, ready for training). If anything is wrong, off-topic,
unsafe, or low quality, lower the score and explain how to fix it.

Output STRICT JSON only, no prose, no markdown fences:
{"passed": <true|false>, "score": <1-10>, "critique": "<how to improve, empty if perfect>"}
